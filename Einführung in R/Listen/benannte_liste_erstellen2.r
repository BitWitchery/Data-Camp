# Benannte Liste erstellen (2)

# Da du ein großer Filmfan bist (und bei Lucasfilm arbeitest), willst du Daten zu guten 
# Filmen mithilfe von Listen speichern.

# Als Erstes erstellst du daher eine Liste für den Film „The Shining“. 
# Wir haben bereits die Variablen mov, act und rev in deinem R-Workspace erstellt. 
# Du kannst sie dir gerne in der Konsole anschauen.

#######################################################################################################

# Vervollständige den Code im Editor, um shining_list zu erstellen; die Liste enthält drei Elemente:

#     moviename: eine Zeichenkette mit dem Filmtitel (gespeichert in mov)
    
#     actors: ein Vektor mit den Namen der Hauptdarsteller (gespeichert in act)
    
#     reviews: ein Dataframe mit einigen Rezensionen (gespeichert in rev)

# Vergiss nicht, die Listenkomponenten entsprechend zu benennen 
# (die Namen sind moviename, actors und reviews).

# The variables mov, act and rev are available

# Finish the code to build shining_list
shining_list <- list(moviename = mov, actors = act, reviews = rev)