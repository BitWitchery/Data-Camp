# Einen Vektor erstellen (2)

# Jetzt müssen wir uns erst mal konzentrieren!

# Auf deinem Weg vom Tellerwäscher zum Millionär wirst du ausgiebig Gebrauch von Vektoren machen. 
# Vektoren sind eindimensionale Arrays, die numerische Daten, Zeichenketten oder logische Daten enthalten können. 
# Mit anderen Worten: Ein Vektor ist ein einfaches Werkzeug, um Daten zu speichern. 
# Du kannst zum Beispiel deine täglichen Gewinne und Verluste in den Casinos speichern.

# In R erstellst du einen Vektor mit der Funktion c() (das c steht für „combine“). 
# Du gibst die Vektorelemente durch Komma getrennt in den Klammern an. Zum Beispiel:

numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")

# Sobald du diese Vektoren in R erstellt hast, kannst du sie für Berechnungen verwenden.

##################################################################################################################

# Vervollständige den Code so, dass boolean_vector folgende drei Elemente enthält: 
# TRUE, FALSE und TRUE (in dieser Reihenfolge).

numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <-c(TRUE, FALSE, TRUE)