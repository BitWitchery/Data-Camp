# Funktionsweise

# Im Editor solltest du R-Code eingeben, um die Aufgaben zu lösen. Wenn du deinen Code einreichst, 
# wird jede Codezeile von R interpretiert und ausgeführt. Außerdem erhältst du eine Meldung, 
# ob dein Code korrekt war oder nicht. Die Ausgabe deines R-Codes wird in der Konsole angezeigt.

# In R können mithilfe des #-Zeichens Kommentare eingefügt werden, um den entsprechenden Codeabschnitt 
# zu erläutern. Das Zeichen kennst du sicher von Hashtags auf Twitter. Kommentare werden nicht als 
# R-Code ausgeführt, sodass sie das Ergebnis nicht beeinflussen. Zum Beispiel ist Calculate 3 + 4 
# („Berechne 3 + 4“) im Editor ein Kommentar.

# Du kannst R-Befehle auch direkt in der Konsole ausführen. Dies ist eine gute Möglichkeit, um mit 
# R-Code zu experimentieren und um zu testen, ob dein eingegebener Code auch korrekt funktioniert.

##########################################################################################################

# Im Editor gibt es bereits Beispielcode. Kannst du erkennen, bei welchen Zeilen es sich um echten 
# R-Code handelt und welche Zeilen Kommentare sind?
    
# Füge eine Codezeile hinzu, die die Summe aus 6 und 12 berechnet, und sende deinen Code ab.

# Calculate 3 + 4
3 + 4

# Calculate 6 + 12
6 + 12
